services:
  - type: web
    name: encryption-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: FERNET_KEY
        generateValue: false  # Renderのダッシュボードで手動設定
      - key: PORT
        value: 10000